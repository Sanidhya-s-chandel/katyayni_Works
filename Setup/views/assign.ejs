<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Assign Camera to User</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://cdn.jsdelivr.net/npm/remixicon/fonts/remixicon.css" rel="stylesheet">
</head>
<body class="bg-gradient-to-r from-blue-50 via-gray-100 to-green-50 flex items-center justify-center min-h-screen px-4">
  <div class="bg-white p-8 rounded-lg shadow-lg w-full max-w-lg">
    <h1 class="text-3xl font-bold text-gray-800 mb-6 text-center flex items-center justify-center">
      <i class="ri-user-line text-blue-500 mr-2"></i>
      Assign Camera to User
    </h1>
    <form action="/assign-camera" method="POST" class="space-y-6">
      <!-- User Name -->
      <div>
        <label for="userName" class="block text-gray-600 font-medium mb-2"> 
          <i class="ri-user-fill text-gray-500 mr-2"></i>
          User Name
        </label>
        <input 
          type="text" 
          id="userName" 
          name="userName" 
          class="w-full px-4 py-3 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" 
          placeholder="Enter User Name" 
          required>
      </div>

      <!-- Camera Name -->
      <div>
        <label for="cameraName" class="block text-gray-600 font-medium mb-2"> 
          <i class="ri-camera-fill text-gray-500 mr-2"></i>
          Camera Name
        </label>
        <input 
          type="text" 
          id="cameraName" 
          name="cameraName" 
          class="w-full px-4 py-3 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" 
          placeholder="Enter Camera Name" 
          required>
      </div>

      <!-- Submit Button -->
      <div>
        <button 
          type="submit" 
          class="w-full bg-blue-500 text-white py-3 px-4 rounded-lg hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 flex items-center justify-center">
          Assign Camera
          <i class="ri-save-line text-xl mr-2"></i>
        </button>
      </div>
    </form>
  </div>
</body>
</html>